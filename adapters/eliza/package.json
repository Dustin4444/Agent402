{
  "name": "elizaos-plugin-agent402",
  "version": "0.2.0",
  "description": "elizaOS plugin for Agent402: find and call pay-per-call web tools (search, page render, PDFs, market and SEC data) over x402 or MPP, paid in USDC or by prepaid card credits with per-call and daily spend ceilings; Agentic Finance for elizaOS agents.",
  "type": "module",
  "main": "index.js",
  "types": "index.d.ts",
  "exports": {
    ".": {
      "types": "./index.d.ts",
      "default": "./index.js"
    }
  },
  "files": [
    "index.js",
    "index.d.ts",
    "README.md"
  ],
  "scripts": {
    "test": "node test.js && node test-runtime.js"
  },
  "keywords": [
    "elizaos",
    "elizaos-plugin",
    "agent402",
    "x402",
    "mpp",
    "aifi",
    "agentic-finance",
    "tools",
    "usdc",
    "mcp"
  ],
  "license": "MIT",
  "author": "Havok Holdings LLC",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/MikeyPetrillo/Agent402.git",
    "directory": "adapters/eliza"
  },
  "homepage": "https://agent402.tools/guides/agent-hosts",
  "dependencies": {
    "agent402-client": "^0.8.4"
  },
  "peerDependencies": {
    "@x402/evm": ">=2.0.0",
    "@x402/fetch": ">=2.0.0",
    "viem": ">=2.0.0"
  },
  "peerDependenciesMeta": {
    "@x402/evm": {
      "optional": true
    },
    "@x402/fetch": {
      "optional": true
    },
    "viem": {
      "optional": true
    }
  },
  "agentConfig": {
    "pluginType": "elizaos:plugin:1.0.0",
    "pluginParameters": {
      "AGENT402_CREDITS_KEY": {
        "type": "string",
        "required": false,
        "sensitive": true,
        "description": "Prepaid card-credits key (a402_...) from https://agent402.tools/credits. Pays wallet-only tools by card."
      },
      "AGENT402_WALLET_KEY": {
        "type": "string",
        "required": false,
        "sensitive": true,
        "description": "EVM private key holding USDC on Base; pays wallet-only tools over x402 (needs @x402/fetch, @x402/evm, viem installed)."
      },
      "AGENT402_BASE_URL": {
        "type": "string",
        "required": false,
        "default": "https://agent402.tools",
        "description": "Gateway base URL (self-hosters point this at their own instance)."
      },
      "AGENT402_MAX_PER_CALL_USD": {
        "type": "string",
        "required": false,
        "default": "1",
        "description": "Ceiling on a single paid call in USD; a call priced above it is refused before paying."
      },
      "AGENT402_DAILY_LIMIT_USD": {
        "type": "string",
        "required": false,
        "description": "Ceiling on rolling 24-hour paid spend in USD, kept across calls for the runtime's lifetime."
      },
      "AGENT402_MAX_RESULT_CHARS": {
        "type": "string",
        "required": false,
        "default": "12000",
        "description": "Longest tool result (JSON characters) placed in the action's text for the model; longer results are truncated with a note and kept whole in data.result."
      }
    }
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@elizaos/core": ">=1.0.0"
  }
}
