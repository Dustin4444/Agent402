name = "agent402-algorand-relay"
main = "src/index.js"
compatibility_date = "2025-09-01"

# RELAY_TOKEN is set as a Worker secret (not a plaintext var). Set it with:
#   wrangler secret put RELAY_TOKEN
# Then mirror the same value into Railway as ALGORAND_RELAY_TOKEN so
# revenue-live.js and revenue-ledger.js send it on every request.
